# Trabajo Front End

Clone the repository, then start the server first:

```shell
cd blog/backend
npm install dependencies
npm start
```

When the server is up start the frontend:

```shell
cd blog/frontend
yarn add dependencies
yarn start
```

Multipage (with error page), important and normal posts, show in detail posts clicking in.
